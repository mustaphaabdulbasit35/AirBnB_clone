def greet(line):
    """function to greet the user."""
    greetings = f"hi, {line}! how are you doing?"
    print(greetings)

    if __name__ = "__main__":
        the_user = input("enter your name: ")
        greet(the_user)
